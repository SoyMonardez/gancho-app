# Copia este archivo como .env y completa los valores
# cp .env.example .env

# ContraseÃ±a root de MySQL (solo usada internamente por el contenedor)
DB_ROOT_PASSWORD=change-me

# Usuario de la aplicaciÃ³n en MySQL
DB_USER=genesis_user

# ContraseÃ±a del usuario de la aplicaciÃ³n
DB_PASSWORD=change-me
