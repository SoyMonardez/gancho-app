# Copia este archivo como .env y completa los valores
# cp .env.example .env

# Contraseña root de MySQL (solo usada internamente por el contenedor)
DB_ROOT_PASSWORD=cambia_esto_root

# Usuario de la aplicación en MySQL
DB_USER=genesis_user

# Contraseña del usuario de la aplicación
DB_PASSWORD=cambia_esto_password
